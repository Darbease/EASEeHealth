# Apps

Frontend clients:
- `provider-portal`
- `payer-ops-console`
- `patient-consent-app`

Each app should consume shared schemas from `packages/schemas` and send correlation IDs with every request.
